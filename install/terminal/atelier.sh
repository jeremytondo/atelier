#!/bin/bash

# TODO: Delete this old stuff. Need to add something to the path anyway so
# may as well not do this crazy.
#
# Creates a simlink to the Atelier executable in the .local/bin directory.
# This makes it easy to run the app from the command line.

# Install directory
# INSTALL_DIR="$HOME/.local/bin"
#
# # Create the install directory if it doesn't exist
# if [ ! -d "$INSTALL_DIR" ]; then
#   echo "Creating directory: $INSTALL_DIR"
#   mkdir -p "$INSTALL_DIR"
# fi
#
# # Create the symlink to the Atelier executable
# ln -s "$HOME/.local/share/atelier/bin/atelier" "$INSTALL_DIR/atelier"
